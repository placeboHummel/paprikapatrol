.has-text-centered.title.is-6= @recipe.title
= render "admin/menu"
.max-width
  .box
    .card
      %figure.image
        = image_tag @recipe.main_image_show
    %br
    = render partial: "form", locals: {url: recipe_update_path}